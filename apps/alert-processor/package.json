{
  "name": "alert-processor",
  "private": true,
  "scripts": {
    "prisma:generate": "bash -lc 'export $(grep -v '^#' ../../.env | xargs) && prisma generate'",
    "prisma:migrate:dev": "bash -lc 'export $(grep -v '^#' ../../.env | xargs) && prisma migrate dev'",
    "prisma:validate": "bash -lc 'export $(grep -v '^#' ../../.env | xargs) && prisma validate'"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.10",
    "@nestjs/config": "^4.0.2",
    "@nestjs/core": "^11.0.10",
    "@nestjs/platform-express": "^11.0.10",
    "@prisma/client": "^6.19.0",
    "amqplib": "^0.10.9",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "prisma": "^6.19.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.8"
  }
}
